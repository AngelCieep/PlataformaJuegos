{% extends 'base.html.twig' %}

{% block title %}Juego{% endblock %}

{% block body %}
    <h1>Juego</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ juego.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ juego.nombre }}</td>
            </tr>
            <tr>
                <th>TokenJuego</th>
                <td>{{ juego.tokenJuego }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ juego.description }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ juego.estado ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_juego_index') }}">back to list</a>

    <a href="{{ path('app_juego_edit', {'id': juego.id}) }}">edit</a>

    {{ include('juego/_delete_form.html.twig') }}
{% endblock %}
