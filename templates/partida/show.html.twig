{% extends 'layout.html.twig' %}

{% block title %}Partida{% endblock %}

{% block page_content %}
    <h1>Partida</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partida.id }}</td>
            </tr>
            <tr>
                <th>Puntos</th>
                <td>{{ partida.puntos }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ partida.fecha ? partida.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_partida_index') }}">back to list</a>

    <a href="{{ path('app_partida_edit', {'id': partida.id}) }}">edit</a>

    {{ include('partida/_delete_form.html.twig') }}
{% endblock %}
